{
  "name": "qa",
  "scripts": {
    "webdriver-clean": "webdriver-manager clean",
    "webdriver-update": "webdriver-manager update",
    "webdriver-start": "webdriver-manager start",
    "e2e-dev-local": "protractor protractor.conf.js",
    "e2e-dev-selenoid": "protractor protractor_selenoid.conf.js"
  },
  "private": true,
  "devDependencies": {
    "@types/jasminewd2": "~2.0.3",
    "jasmine-core": "~3.4.0",
    "protractor": "^7.0.0"
  }
}
